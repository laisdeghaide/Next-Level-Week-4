import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <App />, //componente do react
  document.getElementById('root') //busca lá no html um id que seja root e dentro dele jogue o código acima
);

